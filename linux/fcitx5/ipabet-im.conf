[InputMethod]
Name=IPAbet
Icon=ipabet
# The IPA is not a language, and fcitx5 groups input methods by language code.
# An empty code keeps IPAbet out of every language's group, so it is added
# deliberately rather than turning up under whichever language is configured.
LangCode=
Addon=ipabet
Label=IPA
Configurable=False
