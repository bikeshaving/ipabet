[package]
name = "ipabet_engine"
version = "0.1.0"
edition = "2024"

[lib]
name = "ipabet_engine"
crate-type = ["lib", "staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-normalization = "0.1"
